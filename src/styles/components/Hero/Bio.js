// Import Dependencies
import styled from 'styled-components';

// Import Globals
import * as vars from '../../global/vars';

const Bio = styled.h3`
  color: ${vars.colors.white};
  font-size: ${vars.fontSize.xxxxxxLarge};
  line-height: 1.2;
`;

export default Bio;
